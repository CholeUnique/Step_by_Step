# 工作日志

## 时间
2026-04-03 (本次执行)

## 本次目标
重构 Visualizer 右侧可视区，升级为结构语义可视化系统（Semantic Visualizer）

## 完成内容
- 新增 `VisualizerAdapter`，实现 array / stack / queue / object / primitive / tree / linkedlist 类型识别
- 新增 `VisualizerView`，按类型渲染横向数组方块、垂直栈、队列箭头、对象键值卡片、原始值行
- 修改 `Visualizer.jsx`：移除旧 `VarCards` 函数，接入 `VisualizerView`（变量解析必须经过 Adapter，不直接在 UI 解析）
- 未修改 InterpreterController、Adapter、TimelineStore、App.jsx、CodeEditor.jsx、Toolbar.jsx、WatchPanel.jsx

## 新增文件
- `src/visualizer/VisualizerAdapter.js`
- `src/visualizer/VisualizerView.jsx`
- `logs/2026-04-03_TaskLog.md`

## 修改文件
- `src/components/Visualizer.jsx`（替换变量渲染区，保留步骤计数、进度条、状态标签、调用栈）

## 当前效果
- 数组以横向索引方块展示（蓝色边框）
- 栈以垂直结构展示，顶部高亮（紫色边框）
- 队列以横向箭头连接展示（绿色标注 front/rear）
- 对象以圆角表格键值对展示（橙色边框）
- 原始值单行显示名称=值+类型标签

## 下步计划
- 引入 Framer Motion 实现 fade-in / 高亮闪烁动画
- 增加链表节点连线（linkedlist）与树节点（tree）的图形渲染
- 增加 CSS transition 高亮变化检测

---

## 时间
2026-04-03 第二次执行（edit2.md 任务）

## 本次目标
ES6 支持（Babel 转译）+ 数据结构可视化升级（diff-based stack/queue + React Flow 渲染 tree/linkedlist）

## 完成内容
- 安装依赖：`@babel/standalone`、`reactflow`
- 新增 `src/core/codeTransformer.js`：调用 Babel.transform 将 ES6+ 转为 ES5，失败时回退原始代码
- 修改 `InterpreterController.js`：在 `init()` 中调用 `transformCode()` 后再传给 js-interpreter
- 重写 `VisualizerAdapter.js`（v2）：
  - 接受 `currentSnap` + `previousSnap` 双参数
  - stack/queue 通过 diff 判断（不依赖变量名）：
    - push/pop：尾部变化且前缀匹配 → stack
    - shift：头部元素变化且其余匹配 → queue
  - linkedlist 检测：`{ val/value, next }` 结构
  - tree 检测：`{ val/value, left, right }` 结构
  - 新增 `listToFlow()` 和 `treeToFlow()` 导出函数供 VisualizerView 使用
- 重写 `VisualizerView.jsx`（v2）：
  - linkedlist / tree → React Flow 画布渲染（节点+带箭头连线）
  - array / stack / queue / object / primitive → CSS block 渲染（保留原有样式）
  - SectionLabel 新增 meta.op 标签显示（push/pop/shift/enqueue）
- 修改 `Visualizer.jsx`：读取 `prevSnap`，传递 `prevVariables` 给 VisualizerView

## 新增文件
- `src/core/codeTransformer.js`

## 修改文件
- `src/core/InterpreterController.js`（仅 init() 增加 transformCode 调用）
- `src/visualizer/VisualizerAdapter.js`（全量重写 v2）
- `src/visualizer/VisualizerView.jsx`（全量重写 v2）
- `src/components/Visualizer.jsx`（增加 prevSnap 读取 + prevVariables 传参）

## 当前效果
- 用户可写 ES6（箭头函数、let/const、默认参数）自动转译后执行
- 链表 `{ val, next }` 结构渲染为 React Flow 水平节点链
- 二叉树 `{ val, left, right }` 结构渲染为 React Flow 树形图
- stack/queue 通过前后帧 diff 自动识别，不依赖变量名

## 下步计划
- 引入 Framer Motion 动画（新增/变化节点高亮）
- 增加 queue enqueue 区分（当前首次出现数组默认 array，后续 push 才升级为 stack）

---

## 时间
2026-04-03 第三次执行（edit3.md 任务）

## 本次目标
修复 step 卡在 AST 节点问题 + 修复 React Flow tree/linkedlist 不渲染问题

## 根因分析
1. **step 卡 AST 节点**：`InterpreterController.step()` 每次只调用一次 `interpreter.step()`，而 js-interpreter 是 AST 粒度，同一行可能有 5~20 个 AST 节点，导致用户按一次 Step 行号不变
2. **treeToFlow id 错位 bug**：`traverse(node.left, ...)` 调用前保存 `leftId = String(idCounter)`，但 traverse 内部会先把根节点的 id assign 给 `idCounter++`，导致 leftId 指向的其实是左子树的根，而 edges 的 target 是错误 id
3. **ReactFlow 不渲染**：`ReactFlow` 在 v11 需要 `ReactFlowProvider` 包裹；同时父容器必须有明确的像素高度（百分比高度不生效）
4. **MarkerType**：原代码用字符串 `'arrowclosed'`，需通过 `MarkerType.ArrowClosed` 枚举（或直接用字符串，但 type 字段格式有误）

## 修复内容

### InterpreterController.js
- 新增 `getCurrentLine(interp)` 函数：从 stateStack 顶部提取当前行号
- 新增 `stepToNextLine()` 函数：循环调用 `interpreter.step()` 直到行号变化或执行结束（内循环上限 2000 AST 步防死循环）
- `step()` 改为调用 `stepToNextLine()`，实现 statement-level stepping

### VisualizerAdapter.js
- **修复 treeToFlow id 错位**：改为先分配当前节点 id（`idCounter++`），再用 `String(idCounter)` peek 子节点将被分配的 id，然后才调用 traverse 递归——确保 edge target 与实际子节点 id 一致
- 新增节点 type 字段：`glassNode`（普通节点）/ `nullNode`（null 哨兵）
- listToFlow 补充 null 哨兵节点 + `next` edge label
- treeToFlow edges 补充 `left`/`right` label

### VisualizerView.jsx
- 新增 `GlassNode` 自定义节点：Glassmorphism 样式（毛玻璃背景、白色边框、monospace 字体）
- 新增 `NullNode` 自定义节点：虚线边框 + ∅ 符号
- 新增 `FlowCanvas` 共享组件：封装 `ReactFlowProvider` + 固定像素高度容器 + `Background` 点状网格
- `LinkedListBlock` / `TreeBlock` 改用 `FlowCanvas`
- tree 高度根据深度动态计算（`depth * 100 + 60`）

## 未修改
- `Adapter.js`、`timelineStore.js`、`App.jsx`、`CodeEditor.jsx`、`Toolbar.jsx`、`WatchPanel.jsx`
